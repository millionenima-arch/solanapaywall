services:
  - type: web
    name: solanapaywall
    env: python
    plan: free
    buildCommand: "pip install -r requirements.txt"
    startCommand: "python bot.py"
    envVars:
      PYTHON_VERSION: 3.10
